import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

//in every react application we have to mount he app into the root
ReactDOM.render(<App />, document.getElementById('root'));